set_charging_plan:
  fields:
    vin:
      required: true
      example: "LGWTEST00XX000001"
      selector:
        text:
    start_time:
      required: true
      selector:
        datetime:
    end_time:
      required: true
      selector:
        datetime:

clear_charging_plan:
  fields:
    vin:
      required: true
      example: "LGWTEST00XX000001"
      selector:
        text:
